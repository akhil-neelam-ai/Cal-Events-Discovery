# Optional: client error tracking via Sentry.
# Leave unset to disable (Sentry code is then never loaded — zero bundle cost).
# To enable: create a Sentry project (React platform) and paste its DSN here,
# and add the same variable to the Vercel project environment.
VITE_SENTRY_DSN=
