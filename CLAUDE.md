# CLAUDE.md

Guidance for Claude Code when working in this repository.

The full instructions are shared with every coding agent and live in `AGENTS.md`. It is imported below rather than duplicated, so the two files cannot drift apart.

@AGENTS.md
